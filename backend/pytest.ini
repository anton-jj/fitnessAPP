[pytest]
testpaths = tests

# Put the backend root on sys.path so `from app...` resolves however pytest is
# invoked. Running `python -m pytest` adds the working directory implicitly and
# a bare `pytest` does not — without this, tests pass locally and fail in CI.
pythonpath = .
