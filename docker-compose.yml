services:
  api:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    volumes:
      - pulse-data:/app/data
    ports:
      - "8000:8000"

  web:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - api

volumes:
  pulse-data:
